<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-55G6CL815X"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-55G6CL815X');
</script>
  <title>Best of Hokkien Mee Map</title>

  <style>
    body {
      margin: 0;
      font-family: sans-serif;
    }
    #map {
      height: 60vh;
    }
    .filters {
      display: flex;
      gap: 1rem;
      padding: 1rem;
      background: #f8f8f8;
    }
    .filters a {
      padding: 0.5rem 1rem;
      background: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
    }
    .filters a:hover {
      background: #0056b3;
    }
    .list {
      padding: 1rem;
    }
    .card {
      border: 1px solid #ccc;
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1rem;
    }
    .card h3 {
      margin-top: 0;
    }
    .tag {
      display: inline-block;
      margin-right: 0.5rem;
      background: #eee;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
    }
  </style>
</head>
<body>
  <div class="filters">
    <a href="filter.html?tag=wet">Wet</a>
    <a href="filter.html?tag=dry">Dry</a>
    <a href="filter.html?tag=sambal">Sambal</a>
    <a href="filter.html?tag=cut chilli">Cut Chilli</a>
    <a href="filter.html?tag=wok hei">Wok Hei</a>
    <a href="filter.html?tag=lard">Pork Lard</a> 
  </div>
<script async
  src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap">
</script>

  <script>
    let map;

    function initMap() {
      map = new google.maps.Map(document.getElementById("map"), {
        center: { lat: 1.3521, lng: 103.8198 },
        zoom: 12,
      });

      fetch("HKMlocations.csv")
        .then((response) => response.text())
        .then((csvText) => {
          Papa.parse(csvText, {
            header: true,
            skipEmptyLines: true,
            complete: function (results) {
              const data = results.data;
              const listContainer = document.getElementById("location-list");

              data.forEach((loc) => {
                const lat = parseFloat(loc.lat);
                const lng = parseFloat(loc.lng);
                const marker = new google.maps.Marker({
                  position: { lat, lng },
                  map: map,
                  title: loc.name,
                });

                const infoWindow = new google.maps.InfoWindow({
                  content: `<b>${loc.name}</b><br><a href="${loc.google_maps_url}" target="_blank">View on Google Maps</a>`,
                });

                marker.addListener("click", () => {
                  infoWindow.open(map, marker);
                });

                // Add to list
                const card = document.createElement("div");
                card.className = "card";
                card.innerHTML = `
                  <h3>${loc.name}</h3>
                  <p><strong>Rating:</strong> ${loc.rating} ⭐ (${loc.review_count} reviews)</p>
                  <p><strong>Description:</strong> ${loc.description}</p>
                  <p><strong>Town:</strong> ${loc.town}</p>
                  <p><strong>Operating Hours:</strong> ${loc.operating_hours}</p>
                  <p><strong>Starting Price:</strong> $${parseFloat(loc.price).toFixed(2)}</p>
                  <p><strong>Tags:</strong> ${loc.tags.split(";").map(tag => `<span class="tag">${tag}</span>`).join(" ")}</p>
                  <p><a href="${loc.google_maps_url}" target="_blank">📍 Google Maps</a></p>
                `;
                listContainer.appendChild(card);
              });
            },
          });
        });
    }
  </script>

</body>
</html>
  